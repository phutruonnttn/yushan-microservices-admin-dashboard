# Development API Configuration
# Use relative paths to go through the dev server proxy (setupProxy.js)
# This bypasses CORS issues by making requests appear to come from the same origin
REACT_APP_API_BASE_URL=/api/v1
